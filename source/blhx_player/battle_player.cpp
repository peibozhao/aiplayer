
#include "battle_player.h"

bool BattlePlayer::Init(const std::string &cfg) { return true; }

bool BattlePlayer::Play(const std::vector<ObjectBox> &objects, const std::vector<TextBox> &texts,
                        std::vector<PlayOperation> &operations) {
    return true;
}

bool BattlePlayer::GetLimit() { return true; }
